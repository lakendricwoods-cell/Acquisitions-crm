import { Suspense } from 'react'
import ToolWorkspace from '../tool-workspace' // Adjust relative path if your file is nested differently (e.g. '@/components/tool-workspace')

function LoadingBuyerBlast() {
  return (
    <div
      style={{
        minHeight: 300,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 24,
        color: 'rgba(255,255,255,0.45)',
        fontSize: 13,
      }}
    >
      Loading buyer blast...
    </div>
  )
}

export default function BuyerBlastPage() {
  return (
    <Suspense fallback={<LoadingBuyerBlast />}>
      <ToolWorkspace slug="buyer-blast" />
    </Suspense>
  )
}
