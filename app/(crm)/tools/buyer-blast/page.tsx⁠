import { Suspense } from 'react';
import MyWorkspaceComponent from '@/components/MyWorkspaceComponent';

export default function PageWrapper() {
  return (
    <Suspense fallback={<div className="min-h-screen bg-[#0B0B0E] flex items-center justify-center text-amber-400">Loading Luxury Workspace...</div>}>
      <MyWorkspaceComponent />
    </Suspense>
  );
}


import ToolWorkspace from '../tool-workspace'

function Loading() {
  return (
    <div
      style={{
        minHeight: 300,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 24,
        color: 'rgba(255,255,255,0.45)',
        fontSize: 13,
      }}
    >
      Loading buyer workspace...
    </div>
  )
}

export default function BuyerBlastPage() {
  return (
    <Suspense fallback={<Loading />}>
      <ToolWorkspace slug="buyer-blast" />
    </Suspense>
  )
}